BENEDICT INTERACTIVE — VISIBLE TRANSFER FILES

These filenames are intentionally visible for Android/file-picker compatibility.
DO NOT keep these transfer filenames in the repository.

Canonical destination mapping:

01_quality.yml
→ .github/workflows/quality.yml

02_nvmrc.txt
→ .nvmrc

03_gitignore.txt
→ .gitignore

04_editorconfig.txt
→ .editorconfig

05_vscode_extensions.json
→ .vscode/extensions.json

Important:
GitHub Actions only recognizes workflow files under .github/workflows/.
The dot-prefixed canonical paths are required by the tooling; they cannot be
renamed to non-dot paths in the final repository.
